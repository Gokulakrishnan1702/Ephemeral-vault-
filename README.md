# Ephemeral-vault-
Ephemeral Vault is a zero-knowledge, self-destructing secret sharing platform engineered with AES-256-GCM encryption, SQLite WAL atomic concurrency, and scraper bot defense. It enables secure transmission of credentials via authenticated, cross-network public HTTPS links that permanently self-destruct from storage immediately after a verified view.
